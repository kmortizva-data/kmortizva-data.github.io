# Python
__pycache__/
*.pyc

# System
Thumbs.db
desktop.ini

# Chrome's scratch profile, created by src/shoot.py when it takes the screenshots.
assets/.chrome-profile/

# Local preview config for the coding agent. With .nojekyll, Pages would publish it.
.claude/

# Working folders. check_history.py rejects these too, but a guard that fires at
# push time is the second net: this one stops them at git add, before anything
# of theirs can enter a commit and have to be rewritten out of it.
.venv/
node_modules/
_*/
